import { Units } from "@turf/helpers";

/** minimum rectangle width value in miles */
export const MIN_RECOMMENDED_MIN_WIDTH = 2;
export const MAX_RECOMMENDED_MIN_WIDTH = undefined;
export const MIN_WIDTH_UNIT: Units = "kilometers";
